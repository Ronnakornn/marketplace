# TODO List for Milestone 8

- [x] task-1: implement catalog service spec validation helpers for active category specs, required checks, type normalization, duplicate/wrong-spec rejection, and focused service tests
- [x] task-2: enforce spec validation in seller product create/update/submit-review flows while preserving existing free-form attributes and historical inactive attributes
- [x] task-3: validate public `attributeFilters` against active filterable category specs for category-scoped listing/search endpoints and add route/service tests
- [x] task-4: update seller product studio category spec controls to render type-aware inputs for number/boolean/text-like specs with required hints and UI tests
- [x] task-5: run milestone verification including Prisma generate, typecheck, focused tests, full test suite, Browser checks, and write the milestone report
